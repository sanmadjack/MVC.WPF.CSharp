<UserControl x:Class="MVC.WPF.ModelListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" MouseDown="UserControl_MouseDown">
    <Grid Background="Transparent" MouseDown="UserControl_MouseDown">
        <ScrollViewer Name="Scroller" Background="Transparent">
            <StackPanel Orientation="Vertical" Name="ItemStack" Background="Transparent">
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
